# Timeseries

Per-feed timeseries data

## List available timeseries feeds for a system

`GET /timeseries/available`

```@raw html
<OAOperation operationId="timeseriesAvailable" prefix-headings="true" />
```

## Fetch timeseries data for one or more feeds

`GET /timeseries/data`

```@raw html
<OAOperation operationId="timeseriesData" prefix-headings="true" />
```

