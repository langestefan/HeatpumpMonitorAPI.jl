# System

System metadata and statistics

## Get metadata for a single system

`GET /system/get.json`

```@raw html
<OAOperation operationId="getSystem" prefix-headings="true" />
```

## List public systems

`GET /system/list/public.json`

```@raw html
<OAOperation operationId="listPublicSystems" prefix-headings="true" />
```

## List the logged-in user's systems

`GET /system/list/user.json`

```@raw html
<OAOperation operationId="listUserSystems" prefix-headings="true" />
```

## Stats summary for a custom time window

`GET /system/stats`

```@raw html
<OAOperation operationId="statsCustomWindow" prefix-headings="true" />
```

## Stats summary over all available time

`GET /system/stats/all`

```@raw html
<OAOperation operationId="statsAll" prefix-headings="true" />
```

## Daily stats as CSV

`GET /system/stats/daily`

```@raw html
<OAOperation operationId="statsDaily" prefix-headings="true" />
```

## Stats summary for the last 30 days

`GET /system/stats/last30`

```@raw html
<OAOperation operationId="statsLast30" prefix-headings="true" />
```

## Stats summary for the last 365 days

`GET /system/stats/last365`

```@raw html
<OAOperation operationId="statsLast365" prefix-headings="true" />
```

## Stats summary for the last 7 days

`GET /system/stats/last7`

```@raw html
<OAOperation operationId="statsLast7" prefix-headings="true" />
```

## Stats summary for the last 90 days

`GET /system/stats/last90`

```@raw html
<OAOperation operationId="statsLast90" prefix-headings="true" />
```

## Monthly stats as JSON

`GET /system/stats/monthly`

```@raw html
<OAOperation operationId="statsMonthly" prefix-headings="true" />
```

